import { Item, ItemsType } from '../../@types';

const useCurrentItem = (typeItem: ItemsType, items: Item[]): Item => {
  let item: Item = {} as Item;
  return item;
};

export default useCurrentItem;
